[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q -m "not integration"
markers =
    integration: hits the network (GitHub/sites) — run explicitly with -m integration
