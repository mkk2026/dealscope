# Secrets — never commit
.env
.env.local
*.key
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node / Next.js (frontend, added later)
node_modules/
.next/
out/

# OS / editor
.DS_Store
.idea/
.vscode/

# Build artifacts
dist/
build/

# HF Space deploy files (live in the separate Space repo, not this one)
space/

# Test artifacts
.coverage
htmlcov/
backend/demo/events-test.jsonl

# Internal strategy / design / submission-prep docs — never publish to the public repo
council-report-*.html
council-transcript-*.md
DESIGN-*
SUBMISSION-*
.gstack/
